A good rule from here: we only change code if it supports that contract. No more “nice to have” cleanup unless it blocks the deploy model.

Next, I would do this:

cd /opt/aerolit/aerolit-bundle/collections-hub/ansible

ansible-playbook --syntax-check \
  -i localhost, \
  -c local \
  -e @group_vars/all/aerolitctl_server_generated.yml \
  pxe_server.yml

Then run:

aerolitctl deploy server --dry-run --skip-firewall
aerolitctl deploy server --dry-run

If those show the dnsmasq vars correctly and no syntax/var errors, then we can say:

pxe_content and pxe_dnsmasq are ready enough to move on.

Then we do the exact same contract review for nfs_server.





Yes — these pictures help. For pxe-dnsmasq, I would not change all these files. Most of the role looks fine.

The main cleanup is:

1. defaults/main.yml — remove dangerous hardcoded environment values.
2. tasks/install.yml — strengthen the assert so the role fails clearly if required vars were not provided.
3. Leave these alone for now: tasks/configure.yml, tasks/service.yml, templates/dnsmasq-pxe.conf.j2, templates/dnsmasq-override.conf.j2, tasks/main.yml, and handlers/main.yml.

1. Fix defaults/main.yml

Replace the interface block

Find this:

# Interface / host networking
pxe_dnsmasq_interface: "ens160"
pxe_dnsmasq_bind_mode: "dynamic"
pxe_dnsmasq_except_interfaces: []

Replace with:

# Interface / host networking
# Required. aerolitctl configure server generates this from the selected provisioning NIC.
pxe_dnsmasq_interface: ""
pxe_dnsmasq_bind_mode: "dynamic"
pxe_dnsmasq_except_interfaces: []

Replace the DHCP range block

Find this:

# DHCP range for isolated PXE network
pxe_dnsmasq_dhcp_range_start: "192.168.109.100"
pxe_dnsmasq_dhcp_range_end: "192.168.109.200"
pxe_dnsmasq_dhcp_netmask: "255.255.255.0"
pxe_dnsmasq_dhcp_lease_time: "12h"

Replace with:

# DHCP range for isolated PXE network
# Required when pxe_dnsmasq_enable_dhcp is true.
# aerolitctl configure server generates these values.
pxe_dnsmasq_dhcp_range_start: ""
pxe_dnsmasq_dhcp_range_end: ""
pxe_dnsmasq_dhcp_netmask: ""
pxe_dnsmasq_dhcp_lease_time: "12h"

Replace the PXE handoff block

Find this:

# HTTP handoff to the pxe-content service
pxe_dnsmasq_pxe_server_ip: "192.168.109.139"
pxe_dnsmasq_tftp_server_ip: "{{ pxe_dnsmasq_pxe_server_ip }}"
pxe_dnsmasq_http_port: 18080
pxe_dnsmasq_boot_script_url: "http://{{ pxe_dnsmasq_pxe_server_ip }}:{{ pxe_dnsmasq_http_port }}/boot.ipxe"

Replace with:

# HTTP handoff to the pxe-content service
# Required. aerolitctl configure server generates the server IP and port.
pxe_dnsmasq_pxe_server_ip: ""
pxe_dnsmasq_tftp_server_ip: "{{ pxe_dnsmasq_pxe_server_ip }}"
pxe_dnsmasq_http_port: 18080
pxe_dnsmasq_boot_script_url: "http://{{ pxe_dnsmasq_pxe_server_ip }}:{{ pxe_dnsmasq_http_port }}/boot.ipxe"

Change the firewall default

Find this:

# Firewall
pxe_dnsmasq_manage_firewall: true

Change only that first line to:

# Firewall
# Safe standalone default. aerolitctl generated vars enable this when firewall intent is enabled.
pxe_dnsmasq_manage_firewall: false

Leave the rest of that firewall block alone:

pxe_dnsmasq_firewall_zone: "public"
pxe_dnsmasq_open_tftp_port: true
pxe_dnsmasq_open_http_port: true
pxe_dnsmasq_extra_firewall_ports: []
pxe_dnsmasq_firewall_packages:
  - firewalld
pxe_dnsmasq_firewall_base_ports:
  - "67/udp"
pxe_dnsmasq_firewall_tftp_ports:
  - "69/udp"

2. Fix tasks/install.yml

In tasks/install.yml, replace the first assert task.

Find this:

- name: Assert required PXE server IP is set
  assert:
    that:
      - pxe_dnsmasq_pxe_server_ip | length > 0
    fail_msg: "pxe_dnsmasq_pxe_server_ip must be set."

Replace it with this:

- name: Assert required pxe-dnsmasq settings are set
  assert:
    that:
      - pxe_dnsmasq_interface | string | length > 0
      - pxe_dnsmasq_pxe_server_ip | string | length > 0
      - pxe_dnsmasq_tftp_server_ip | string | length > 0
      - (pxe_dnsmasq_http_port | int) >= 1
      - (pxe_dnsmasq_http_port | int) <= 65535
      - not (pxe_dnsmasq_enable_dhcp | bool) or (pxe_dnsmasq_dhcp_range_start | string | length > 0)
      - not (pxe_dnsmasq_enable_dhcp | bool) or (pxe_dnsmasq_dhcp_range_end | string | length > 0)
      - not (pxe_dnsmasq_enable_dhcp | bool) or (pxe_dnsmasq_dhcp_netmask | string | length > 0)
    fail_msg: >-
      pxe-dnsmasq requires interface, PXE server IP, HTTP port, and DHCP range
      settings. When using aerolitctl, run aerolitctl configure server so
      aerolitctl_server_generated.yml can provide these role variables.

3. Do not change these yet

From the pictures, these look okay for our current goal:

tasks/configure.yml
tasks/service.yml
templates/dnsmasq-pxe.conf.j2
templates/dnsmasq-override.conf.j2
tasks/main.yml
handlers/main.yml

The firewall tasks in tasks/configure.yml already do the right thing because they are gated by:

when: pxe_dnsmasq_manage_firewall | bool

That means --skip-firewall can disable them through generated vars, and normal deploy can enable them.

4. Test after those changes

Run this in the pxe-dnsmasq role repo:

ansible-playbook --syntax-check -i localhost, -c local \
  -e @/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_server_generated.yml \
  tests/test.yml

If you do not have a tests/test.yml, then at minimum run this from the bundle Ansible directory:

cd /opt/aerolit/aerolit-bundle/collections-hub/ansible
ansible-playbook --syntax-check \
  -i localhost, \
  -c local \
  -e @group_vars/all/aerolitctl_server_generated.yml \
  pxe_server.yml

Then run the deploy dry-runs again:

aerolitctl deploy server --dry-run --skip-firewall
aerolitctl deploy server --dry-run

After this, I’d call pxe_dnsmasq aligned enough to move on to nfs_server.



































Yes — **this looks right** for the `server.py` side. The uploaded file now has the dnsmasq firewall zone default, the two dnsmasq helper functions, the dnsmasq generated vars, the contract validation, and the configure-server display lines. 

I also checked it as Python, and it **compiles cleanly**.

The only thing I’d change is cosmetic, not functional: add one extra blank line before this line:

```python
def build_generated_vars(plan):
```

Right now it will still run fine, but two blank lines between top-level functions is cleaner.

Now test exactly this:

```bash
python3 -m compileall src tools
```

Then regenerate:

```bash
aerolitctl configure server
```

Then confirm the generated file has the new vars:

```bash
grep -E 'pxe_dnsmasq_(interface|enable_dhcp|enable_tftp|pxe_server_ip|tftp_server_ip|http_port|dhcp_range_start|dhcp_range_end|dhcp_netmask|gateway|dns_servers|manage_firewall|firewall_zone|open_tftp_port|open_http_port|extra_firewall_ports)' \
  /opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_server_generated.yml
```

You want to see values like:

```yaml
pxe_dnsmasq_interface: ens160
pxe_dnsmasq_pxe_server_ip: 192.168.109.10
pxe_dnsmasq_tftp_server_ip: 192.168.109.10
pxe_dnsmasq_http_port: 18080
pxe_dnsmasq_dhcp_range_start: 192.168.109.100
pxe_dnsmasq_dhcp_range_end: 192.168.109.149
pxe_dnsmasq_manage_firewall: true
pxe_dnsmasq_firewall_zone: public
```

Then run:

```bash
aerolitctl deploy server --dry-run --skip-firewall
```

and:

```bash
aerolitctl deploy server --dry-run
```

One important distinction: this fixes the **aerolitctl-generated variables** for `pxe_dnsmasq`. It does not yet clean up the `pxe_dnsmasq` role defaults themselves. That is okay for deploy through `aerolitctl`, because generated vars should override role defaults, but the next cleanup should be making the role defaults less hardcoded so the role is safer standalone too.



























































Yes. A good “done with pxe_content” proof should show three things:

1. Python generates the pxe_content_* values.
2. The pxe_content role consumes only its own role vars.
3. --skip-firewall disables the role firewall path without disabling the whole PXE role.

Run this from the aerolitctl repo:

cd ~/aerolitctl
python3 -m compileall src tools
git diff --check

Both should pass. git diff --check should print nothing.

Then prove the old ownership conflict is gone:

grep -R "aerolitctl_pxe_server_ip\|pxe_dnsmasq_manage_firewall\|enable-dnsmasq-firewall\|disable-dnsmasq-firewall\|dnsmasq-firewall-zone" -n src/aerolitctl || true

Expected result: no output.

Then prove the current generated server vars contain the pxe_content handoff:

grep -E "pxe_content_(server_ip|host_port|image_source|image|runtime_image|image_tar_path|manage_firewall|firewall_ports|volume_mounts|extra_host_dirs)" \
  /opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_server_generated.yml

Expected important lines should look like this:

pxe_content_extra_host_dirs:
pxe_content_firewall_ports:
pxe_content_host_port: 18080
pxe_content_image: registry.gitlab.us.lmco.com/lit/containers/pxe-content:latest
pxe_content_image_source: tar
pxe_content_image_tar_path: /opt/aerolit/aerolit-bundle/files/pxe/pxe-content.tar
pxe_content_manage_firewall: true
pxe_content_runtime_image: registry.gitlab.us.lmco.com/lit/containers/pxe-content:latest
pxe_content_server_ip: 192.168.109.10
pxe_content_volume_mounts:

The exact order does not matter. The important part is that pxe_content_server_ip, pxe_content_host_port, pxe_content_manage_firewall, and pxe_content_firewall_ports are there.

Then run the two deploy dry-runs:

aerolitctl deploy server --dry-run --skip-firewall

You want to see:

Effective deploy mode : disabled by --skip-firewall
Requested packages    : ansible-core, podman, nfs-utils, dnsmasq

Meaning: firewalld is not requested when firewall is skipped.

Then:

aerolitctl deploy server --dry-run

You want to see:

Effective deploy mode : enabled from configure server
Requested packages    : ansible-core, podman, nfs-utils, dnsmasq, firewalld

Meaning: firewalld is requested only when firewall management is enabled.

Then do the Ansible syntax proof:

cd /opt/aerolit/aerolit-bundle/collections-hub/ansible
ansible-playbook \
  -i localhost, \
  -c local \
  --syntax-check \
  -e @group_vars/all/aerolitctl_server_generated.yml \
  pxe_server.yml

Expected:

playbook: pxe_server.yml

If all of that passes, I would call pxe_content done for this cleanup.

The final checkpoint commit would be:

cd ~/aerolitctl
git status
git add src/aerolitctl/cli.py src/aerolitctl/deploy_server.py src/aerolitctl/pxe.py src/aerolitctl/repos.py src/aerolitctl/server.py src/aerolitctl/status.py
git commit -m "Align pxe-content firewall and generated vars ownership"

After that, yes — move to pxe_dnsmasq.






































cd ~/aerolitctl

git diff --check

grep -R "enable-dnsmasq-firewall\|disable-dnsmasq-firewall\|dnsmasq-firewall-zone" -n src/aerolitctl || true

grep -R "aerolitctl_pxe_server_ip\|pxe_dnsmasq_manage_firewall" -n src/aerolitctl || true

aerolitctl deploy server --dry-run --skip-firewall
aerolitctl deploy server --dry-run


















Yes — the GUI shows the issue. You changed it back to DHCP, but DNS/routes are still disabled:

* DNS automatic is OFF
* Routes automatic is OFF
* “Use this connection only for resources on its network” is checked

That means the VM can get an IP like 192.168.98.148, but it still may not have DNS/default route, so git pull cannot resolve gitlab.us.lmco.com.

Run this from the VM console:

nmcli connection modify ens160 \
  ipv4.method auto \
  ipv4.addresses "" \
  ipv4.gateway "" \
  ipv4.dns "" \
  ipv4.ignore-auto-routes no \
  ipv4.ignore-auto-dns no \
  ipv4.never-default no

Then restart the connection:

nmcli connection down ens160
nmcli connection up ens160

Now verify:

ip route | grep default
nmcli device show ens160 | egrep 'IP4.ADDRESS|IP4.GATEWAY|IP4.DNS'
getent hosts gitlab.us.lmco.com

You want to see:

default via 192.168.98.2 ...
IP4.GATEWAY: 192.168.98.2
IP4.DNS[1]: 192.168.98.2

Then pull from the actual repo directory:

cd ~/aerolitctl
git pull

If DNS still does not show up, force the VMware NAT DNS temporarily:

nmcli connection modify ens160 ipv4.dns "192.168.98.2" ipv4.ignore-auto-dns no
nmcli connection down ens160
nmcli connection up ens160
getent hosts gitlab.us.lmco.com

This is just to get internet back temporarily. It will remove the dedicated 192.168.109.10/24 provisioning-only setup until you run the AeroLIT deploy/interface step again.

















This confirms the issue clearly:

ens160 has both:
  192.168.109.10/24   # AeroLIT planned PXE IP
  192.168.98.148/24   # DHCP/old network IP
default route:
  via 192.168.98.2 using 192.168.98.148
DNS:
  192.168.98.2

So AeroLIT added/configured the 192.168.109.10 address, but the interface is still also using DHCP on 192.168.98.x.

What I would do next

First, run this one command so we know whether NetworkManager saved both addresses permanently:

nmcli connection show ens160 | egrep 'ipv4.method|ipv4.addresses|ipv4.gateway|ipv4.dns|ipv4.ignore-auto'

Then inspect the deploy code that configured the interface:

grep -R "ip addr add\|+ipv4.addresses\|ipv4.addresses\|ipv4.method\|configure_interface\|nmcli" -n src/aerolitctl/deploy_server.py

Paste those outputs next.

Don’t manually remove the 192.168.98 address yet

Not until we decide whether ens160 is supposed to be:

A dedicated isolated PXE/provisioning NIC

or:

Your management/internet NIC plus a temporary PXE IP

If ens160 is your only NIC and you remove 192.168.98.148, you may lose internet/GitLab access from the VM.

The likely code issue

If deploy_server.py uses something like this, it is appending:

nmcli connection modify ens160 +ipv4.addresses 192.168.109.10/24

That is why both IPs remain.

For the “clean/right” isolated PXE design, it should use replacement behavior:

nmcli connection modify ens160 \
  ipv4.method manual \
  ipv4.addresses 192.168.109.10/24 \
  ipv4.gateway "" \
  ipv4.dns "" \
  ipv4.ignore-auto-dns yes

But before we make that code change, I want to see the exact current deploy_server.py logic so we do not accidentally break the deploy flow.


























Yeah, I agree. That is probably from the interface configuration step adding the new AeroLIT IP without removing the old one.

Most likely cause:

ens160 already had:      192.168.98.148/24
aerolitctl added:        192.168.109.10/24
result:                  both addresses on ens160

That can happen if the deploy code uses something like ip addr add ... or nmcli ... +ipv4.addresses ... instead of replacing the interface’s IPv4 address list.

First, verify whether the extra IP is temporary or persistent

Run these:

ip -4 addr show dev ens160
ip route
nmcli device show ens160 | egrep 'GENERAL.CONNECTION|IP4.ADDRESS|IP4.GATEWAY|IP4.DNS'
nmcli connection show --active

Then get the active connection name. It may be something like ens160, Wired connection 1, or System ens160.

Then run:

nmcli connection show "<CONNECTION_NAME>" | egrep 'ipv4.method|ipv4.addresses|ipv4.gateway|ipv4.dns|ipv4.ignore-auto'

Replace <CONNECTION_NAME> with the actual active connection name.

What we are looking for

If you see both addresses under ipv4.addresses, then the extra IP is persistent in NetworkManager.

If you see ipv4.method: auto, then 192.168.98.148 is probably DHCP and AeroLIT added 192.168.109.10 on top of it.

If ip addr shows both but nmcli connection show only shows one, then one of them may be temporary/runtime-only.

Manual cleanup for your current VM

Assuming you want ens160 to be only:

192.168.109.10/24

and no gateway/DNS for the isolated PXE network, use:

CONN="$(nmcli -t -f GENERAL.CONNECTION device show ens160 | cut -d: -f2-)"
nmcli connection modify "$CONN" \
  ipv4.method manual \
  ipv4.addresses "192.168.109.10/24" \
  ipv4.gateway "" \
  ipv4.dns "" \
  ipv4.ignore-auto-dns yes \
  ipv6.method disabled
nmcli connection down "$CONN"
nmcli connection up "$CONN"
ip -4 addr show dev ens160

After that, you should only see:

192.168.109.10/24

on ens160.

Code fix we probably need

The deploy interface logic should replace the managed provisioning interface IP, not append to it.

So if the current code uses either of these patterns, that is probably the bug:

ip addr add 192.168.109.10/24 dev ens160

or:

nmcli connection modify "$CONN" +ipv4.addresses 192.168.109.10/24

The correct long-term behavior should be closer to:

nmcli connection modify "$CONN" \
  ipv4.method manual \
  ipv4.addresses "192.168.109.10/24"

No +ipv4.addresses.

For an isolated PXE network with gateway set to none, it should also clear gateway/DNS instead of preserving old DHCP settings:

nmcli connection modify "$CONN" \
  ipv4.gateway "" \
  ipv4.dns "" \
  ipv4.ignore-auto-dns yes

The file we likely need to inspect/fix next is deploy_server.py, specifically the function that configures the interface/IP. Look for anything using:

ip addr add
+ipv4.addresses
nmcli connection modify
ipv4.addresses

That is where this probably comes from.







grep -E 'pxe_content_manage_firewall|pxe_content_firewall_ports|pxe_content_server_ip|pxe_content_image_source|pxe_content_runtime_image|pxe_content_extra_host_dirs' \
  /opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_server_generated.yml









sed -n '/^pxe_content_extra_host_dirs:/,/^pxe_dhcp_start:/p' \
  /opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_server_generated.yml


python3 -m compileall src/aerolitctl












I do feel good about the direction, but I would phrase it this way:

I feel good about the architecture and ownership cleanup. I do not want you to merge it yet until we run the checks, because this touched enough files that we need to prove there are no broken imports, stale CLI args, or missing generated variables.

The big thing: we did not delete all of that just for firewall.

The firewall problem exposed a bigger issue: pxe.py, server.py, deploy_server.py, and the roles were all starting to share ownership of the same things.

The cleanup was really this:

configure server / server.py owns:
  server IP
  PXE HTTP port
  interface
  DHCP/dnsmasq settings
  firewall intent
  role firewall vars
  ISO root
  pxe-content image/tar mode
  role handoff vars
configure pxe / pxe.py owns:
  PXE profiles
  profile overrides
  template overrides
  generator behavior
  PXE menu/Kickstart policy

So yes, the huge deletion from pxe.py makes sense if most of what came out was server IP, dnsmasq, DHCP, HTTP port, ISO root, volume mount, and firewall ownership logic. That stuff did not belong in configure pxe.

The file I would be most suspicious of from that screenshot is repos.py +2048 -0. That does not look like a small firewall cleanup. It may be completely valid if you added the repo auto-discovery work, but I would review that separately from this PXE/firewall change so you do not accidentally mix two large efforts.

Before moving to pxe_dnsmasq, I would run these quick confidence checks:

cd /path/to/your/aerolitctl/repo
python3 -m compileall src/aerolitctl

Then check for deleted CLI/options still being referenced:

grep -R "enable-dnsmasq-firewall\|disable-dnsmasq-firewall\|dnsmasq-firewall-zone" -n src/aerolitctl || true
grep -R "aerolitctl_pxe_server_ip\|pxe_dnsmasq_pxe_server_ip\|pxe_dnsmasq_manage_firewall" -n src/aerolitctl || true

Then regenerate/check server vars and make sure the important role vars are present:

grep -E 'pxe_content_(server_ip|host_port|manage_firewall|firewall_ports|image_source|runtime_image|quadlet_pull_policy|volume_mounts|extra_host_dirs)' \
  /opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_server_generated.yml

And syntax-check Ansible:

cd /opt/aerolit/aerolit-bundle/collections-hub/ansible
ansible-playbook pxe_server.yml --syntax-check

My honest take: yes, the amount deleted is believable, especially the pxe.py -760, because we intentionally moved server/dnsmasq/firewall ownership out of configure pxe. But because cli.py, deploy_server.py, server.py, and pxe.py all changed together, we should validate the Python wiring before doing the next role.

































pxe_content_image: "registry.gitlab.us.lmco.com/lit/containers/pxe-content:latest"

# Image name used at runtime by Quadlet.
# In registry mode this usually matches pxe_content_image.
# In tar mode this must match the tag embedded in the loaded tar.
pxe_content_runtime_image: "{{ pxe_content_image }}"










Almost — the pxe-content role is mostly aligned now, but I would not call pxe-content done yet until you fix/verify these last few things.

The role is good in the big-picture ownership model:

Python/server.py:
  decides values
  writes generated vars
pxe_content role:
  installs/packages
  loads/pulls image
  writes env file
  writes Quadlet file
  starts service
  opens its own firewall port when told to

That part is correct.

What still needs to be fixed or verified for pxe-content

1. Confirm server.py now generates every pxe-content var

Since we removed pxe-content ownership from pxe.py, aerolitctl_server_generated.yml now needs to contain these:

aerolit_bundle_root: "/opt/aerolit/aerolit-bundle"
pxe_content_server_ip: "192.168.109.10"
pxe_content_host_port: 18080
pxe_content_iso_root: "/opt/aerolit/aerolit-bundle/extracted_isos"
pxe_content_volume_mounts:
  - "/opt/aerolit/aerolit-bundle/extracted_isos:/iso:ro,Z"
  - "/etc/aerolit/overrides/pxe:/overrides:ro,Z"
pxe_content_extra_host_dirs:
  - "/etc/aerolit/overrides/pxe"
  - "/etc/aerolit/overrides/pxe/profiles"
  - "/etc/aerolit/overrides/pxe/templates"
pxe_content_extra_env:
  PXE_OVERRIDE_ROOT: "/overrides"
  PXE_OVERRIDE_PROFILE_ROOT: "/overrides/profiles"
  PXE_OVERRIDE_TEMPLATE_ROOT: "/overrides/templates"
  PXE_GENERATED_KS_EDIT_POLICY: "preserve"
  PXE_VALIDATION_FAILURE_POLICY: "warn"
  PXE_WATCH_INTERVAL: "15"
pxe_content_manage_firewall: true
pxe_content_firewall_ports:
  - "18080/tcp"
pxe_content_image_source: "tar"
pxe_content_image_tar_path: "/opt/aerolit/aerolit-bundle/files/pxe/pxe-content.tar"
pxe_content_quadlet_pull_policy: "never"
pxe_content_runtime_image: "WHATEVER_TAG_IS_INSIDE_THE_TAR"

That generated file is the final contract. The role looks like it can consume these vars correctly.

⸻

2. Add pxe_content_extra_host_dirs to role defaults

In roles/pxe-content/defaults/main.yml, add this near the volume mount section:

# Additional host-side directories required by generated volume mounts.
# AeroLIT normally uses this for /etc/aerolit/overrides/pxe and children.
pxe_content_extra_host_dirs: []
# Host directories this role should ensure before Quadlet starts.
pxe_content_required_host_dirs: "{{ [pxe_content_iso_root] + (pxe_content_extra_host_dirs | default([])) }}"

This fixes the issue where the generated Quadlet can mount:

/etc/aerolit/overrides/pxe:/overrides:ro,Z

but the role does not currently ensure that host path exists.

⸻

3. Replace the ISO-root directory task in install.yml

Right now the role only ensures:

pxe_content_iso_root

exists.

Replace the existing task named something like:

- name: Ensure PXE extracted ISO root exists

with this:

- name: Ensure pxe-content host mount directories exist
  file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop: "{{ pxe_content_required_host_dirs }}"
  when:
    - item | string | length > 1
    - item | string != "/"

That makes the role safe for both:

/opt/aerolit/aerolit-bundle/extracted_isos
/etc/aerolit/overrides/pxe
/etc/aerolit/overrides/pxe/profiles
/etc/aerolit/overrides/pxe/templates

⸻

4. Strengthen the ISO-root assert

Your current assert checks that pxe_content_iso_root is longer than 1 and not /. I would replace it with this:

- name: Assert PXE content ISO root is safe
  assert:
    that:
      - pxe_content_iso_root | string | length > 1
      - pxe_content_iso_root | string != "/"
      - pxe_content_iso_root | string is match("^/")
      - "' ' not in (pxe_content_iso_root | string)"
    fail_msg: "pxe_content_iso_root must be an absolute safe directory path and cannot be '/'."

That prevents relative paths from accidentally becoming Quadlet mounts.

⸻

5. Remove the /srv/ipxe fallback if you want the strict long-term design

Your defaults still appear to have this kind of fallback behavior:

pxe_content_extracted_isos_root: "{{ ... fallback to /srv/ipxe ... }}"

For the stricter AeroLIT design, I would not silently fall back to /srv/ipxe.

Use this instead:

pxe_content_bundle_root: "{{ aerolit_bundle_root | default('') }}"
pxe_content_extracted_isos_root: "{{ ((pxe_content_bundle_root | string | length) > 0) | ternary(pxe_content_bundle_root ~ '/extracted_isos', '') }}"
pxe_content_iso_root: "{{ pxe_content_extracted_isos_root }}"

Then the assert catches missing generated vars instead of quietly deploying against the wrong path.

⸻

6. The runtime image still needs final verification

This is the one thing I cannot call done from screenshots alone.

You need to know what tag is embedded inside:

/opt/aerolit/aerolit-bundle/files/pxe/pxe-content.tar

Run this:

tar -xOf /opt/aerolit/aerolit-bundle/files/pxe/pxe-content.tar manifest.json | python3 -m json.tool

Look for RepoTags.

Then server.py should generate:

pxe_content_runtime_image: "<that exact RepoTags value>"

Example:

pxe_content_runtime_image: "pxe-content:latest"

or:

pxe_content_runtime_image: "registry.gitlab.us.lmco.com/lit/containers/pxe-content:latest"

This matters because podman load can succeed but the Quadlet service can still fail if Image= points to a different tag.

⸻

What looks good now

These look right from the screenshots:

pxe_content_image_source: "tar"
pxe_content_server_ip: ""
pxe_content_manage_firewall: true
pxe_content_firewall_ports:
  - "{{ pxe_content_host_port }}/tcp"

And the role behavior looks right:

install.yml:
  validates vars
  installs podman
  installs firewalld only when firewall management is enabled
  loads tar image
  pulls registry image only in registry mode
configure.yml:
  writes env file
  writes Quadlet container file
  starts firewalld only when enabled
  opens pxe-content firewall ports only when enabled
service.yml:
  enables/starts pxe-content
  validates HTTP content
templates:
  pxe-content.env.j2 correctly writes PXE_SERVER and PXE_PORT
  pxe-content.container.j2 correctly uses Image, PublishPort, Volume, EnvironmentFile, Pull

Verdict

Not quite done yet.

For pxe-content, I would do these final items:

1. Add pxe_content_extra_host_dirs / pxe_content_required_host_dirs defaults.
2. Replace the ISO-root directory task with a generic host mount directory task.
3. Strengthen the ISO-root assert to require an absolute path.
4. Remove the /srv/ipxe fallback if you want strict generated-vars ownership.
5. Verify the tar image RepoTags and make server.py generate matching pxe_content_runtime_image.
6. Re-run configure server and confirm aerolitctl_server_generated.yml contains the final pxe_content_* contract.

After that, I’d be comfortable saying pxe-content is aligned and moving to pxe_dnsmasq.
































deploy_nfs(plan, report, dry_run=dry_run, skip=skip_nfs)

controller_will_manage_nfs = bool(
    plan.get("controller_enabled") and not skip_controller
)

if controller_will_manage_nfs and not skip_nfs:
    report.add(
        "skipped",
        "Wrapper NFS deployment skipped because ansible_controller.yml manages NFS",
    )

deploy_nfs(
    plan,
    report,
    dry_run=dry_run,
    skip=(skip_nfs or controller_will_manage_nfs),
)








You fixed the PXE content root problem. The evidence is good:

/opt/aerolit/aerolit-bundle/extracted_isos -> /iso
/etc/aerolit/overrides/pxe -> /overrides
HTTP/1.1 200 OK from /boot.ipxe

Also, don’t worry about this line by itself:

Image=registry.gitlab.us.lmco.com/lit/containers/pxe-content:latest

That is okay as long as the service uses --pull never and the image exists locally. It is using the local image tag now.

But I see two remaining issues before Phase 5 is truly clean.

1. Your boot.ipxe is still pointing clients at the wrong server IP

Your curl works against:

http://192.168.109.10:18080/boot.ipxe

But the file content says:

set server 192.168.109.139

That means a real PXE client may successfully download boot.ipxe, then try to fetch follow-on content from 192.168.109.139, not 192.168.109.10.

Fix that first:

cd /root/aerolitctl
aerolitctl configure pxe \
  --pxe-server-ip 192.168.109.10 \
  --pxe-http-port 18080 \
  --pxe-content-iso-root /opt/aerolit/aerolit-bundle/extracted_isos

Then rerun only the PXE side:

aerolitctl deploy server --server-bootstrap-only --skip-controller

Then confirm the generated boot file is correct:

curl -s http://127.0.0.1:18080/boot.ipxe | grep -E '^set server|^set port|^set base'
curl -s http://192.168.109.10:18080/boot.ipxe | grep -E '^set server|^set port|^set base'

Expected:

set server 192.168.109.10
set port 18080
set base http://${server}:${port}

2. Your full deploy is now failing in ansible_controller.yml, not PXE

This line is the important one:

ERROR: Command failed rc=2: Run ansible_controller.yml

So the PXE role is no longer the main blocker. The next problem is the controller playbook, but aerolitctl is hiding the actual Ansible task failure because it captures stdout.

Run the controller playbook manually with the same vars so we can see the real failure:

cd /opt/aerolit/aerolit-bundle/collections-hub/ansible
export ANSIBLE_CONFIG="$PWD/ansible.cfg"
set -o pipefail
ansible-playbook \
  -i localhost, \
  -c local \
  -e @/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_server_generated.yml \
  -e @/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml \
  /opt/aerolit/aerolit-bundle/collections-hub/ansible/ansible_controller.yml \
  -vv \
  2>&1 | tee /tmp/aerolit-ansible-controller-failure.log
echo "ansible rc=${PIPESTATUS[0]}"

Then pull out the failure:

grep -nE 'FAILED!|fatal:|ERROR|Cannot|No such|permission|denied|metadata|repo|exportfs' \
  /tmp/aerolit-ansible-controller-failure.log | tail -100

Where I’d mark this right now

I would say:

Phase 1: done
Phase 2: done
Phase 3: done
Phase 4: done
Phase 5 repo-preflight wiring: done
Phase 5 full happy-path deploy: not done yet

You’re very close. The repo preflight is working. PXE content serving is working. The immediate fix is the stale 192.168.109.139 in boot.ipxe, then isolate the ansible_controller.yml failure.











































The failure is clear now: pxe-content is running, but it is serving from the wrong content root.

Your service is mounting this:

/srv/ipxe:/iso:ro,Z

But your screenshot shows /srv/ipxe is basically empty. Your real ISO/repo content is here:

/opt/aerolit/aerolit-bundle/extracted_isos

So the container starts, port 18080 opens, but /boot.ipxe never becomes valid. That is why validation retries for 6 minutes and then fails with:

pxe-content did not return expected status 200 from http://127.0.0.1:18080/boot.ipxe

Do this fix next.

cd /root/aerolitctl
aerolitctl configure pxe \
  --pxe-content-iso-root /opt/aerolit/aerolit-bundle/extracted_isos

Then confirm the bad /srv/ipxe value is gone from the generated vars:

grep -RnsE 'pxe_content_(bundle_root|extracted_isos_root|iso_root|image_source|image_tar|tar_src|runtime_image)' \
  /opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all

What you want to see is something like:

pxe_content_extracted_isos_root: /opt/aerolit/aerolit-bundle/extracted_isos
pxe_content_iso_root: /iso
pxe_content_image_source: tar

Then force the service to rebuild with the corrected mount:

systemctl stop pxe-content || true
podman rm -f pxe-content || true
systemctl daemon-reload

Now rerun through aerolitctl, not the manual playbook:

aerolitctl deploy server --server-bootstrap-only

After it runs, check the actual mounted source:

systemctl cat pxe-content | grep -E -- '--volume|Volume|Image|EnvironmentFile|ExecStart'
podman inspect pxe-content \
  --format '{{range .Mounts}}{{println .Source "->" .Destination}}{{end}}'

Expected mount should be:

/opt/aerolit/aerolit-bundle/extracted_isos -> /iso

not:

/srv/ipxe -> /iso

Then verify HTTP:

curl -sv --max-time 10 http://127.0.0.1:18080/boot.ipxe 2>&1 | head -80
curl -sv --max-time 10 http://192.168.109.10:18080/boot.ipxe 2>&1 | head -80

Expected result: HTTP 200, not hanging, not 404, not timeout.

One important thing: do not keep fixing this by manually running the playbook with extra vars. The whole Phase 5 point is that aerolitctl deploy server needs to carry the right vars itself. Right now the remaining issue is not repo Phase 5; it is that the saved/generated PXE content root still points at the old /srv/ipxe style path instead of the bundle path.
























Yes — this is not Phase 5 repo wiring anymore. You made it past that. This is now inside the PXE Ansible role / pxe-content service validation.

The important clue is: it succeeded once, then failed on a rerun. That usually means either:

1. the validation task is running too fast after the service restart, or
2. the service is running but not serving the exact file/content the validation expects, or
3. the role is not fully idempotent around restarting/removing/recreating the podman container.

Do this next. First capture the real failure without the giant service JSON drowning it out:

cd /opt/aerolit/aerolit-bundle/collections-hub/ansible
export ANSIBLE_CONFIG="$PWD/ansible.cfg"
ansible-playbook \
  -i localhost, \
  -c local \
  -e @/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_server_generated.yml \
  -e @/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml \
  /opt/aerolit/aerolit-bundle/collections-hub/ansible/pxe_server.yml \
  2>&1 | tee /tmp/aerolit-pxe-server-rerun.log
echo "ansible rc=${PIPESTATUS[0]}"

Then immediately run:

grep -nE "FAILED|fatal:|Validate pxe-content|Wait for pxe-content|pxe-content is serving" \
  /tmp/aerolit-pxe-server-rerun.log
tail -n 120 /tmp/aerolit-pxe-server-rerun.log

Now check whether the service is actually serving content:

systemctl --no-pager -l status pxe-content
podman ps -a --filter name=pxe-content
podman images | grep -i pxe
curl -sv http://127.0.0.1:18080/boot.ipxe 2>&1 | head -80
curl -sv http://192.168.109.10:18080/boot.ipxe 2>&1 | head -80
ls -lah /srv/ipxe
find /srv/ipxe -maxdepth 3 -type f | sort | head -80
journalctl -u pxe-content --no-pager -n 120

Also show the validation task code:

grep -Rns \
  "Validate pxe-content is serving expected content\|Wait for pxe-content port\|pxe_content_validate\|uri:" \
  /opt/aerolit/aerolit-bundle/collections-hub/collections/ansible_collections/aerolit/deployments/roles/pxe_content/tasks

My bet: the fix will be in this file:

/opt/aerolit/aerolit-bundle/collections-hub/collections/ansible_collections/aerolit/deployments/roles/pxe_content/tasks/service.yml

Specifically, the task named something like:

- name: Validate pxe-content is serving expected content

probably needs retries/delay, because “port is reachable” does not always mean “content is ready.” The service can start, the port can open, and the generator/container may still need a few seconds before /boot.ipxe or the expected content is actually valid.

The pattern I’d want in that role is something like this:

- name: Validate pxe-content is serving expected content
  ansible.builtin.uri:
    url: "http://127.0.0.1:{{ pxe_content_host_port }}/boot.ipxe"
    return_content: true
    status_code: 200
  register: pxe_content_validation
  changed_when: false
  failed_when: false
  retries: 24
  delay: 5
  until:
    - pxe_content_validation.status | default(0) == 200
    - pxe_content_validation.content is defined
    - pxe_content_validation.content | length > 0
- name: Assert pxe-content validation succeeded
  ansible.builtin.assert:
    that:
      - pxe_content_validation.status | default(0) == 200
      - pxe_content_validation.content is defined
      - pxe_content_validation.content | length > 0
    fail_msg: >-
      pxe-content service started, but expected HTTP content was not ready or not served.
      Check /srv/ipxe, /etc/pxe-content/pxe-content.env, and journalctl -u pxe-content.

But don’t paste that blindly yet — first run the grep/tail/curl commands above so we can see whether it’s failing because the endpoint is empty/404, because the container is not actually running, or because the validation is just too impatient.













































Good news: the stale NFS export problem is fixed. The deploy made it past:

exportfs -rav

Now it is failing at the next stage:

Run pxe_server.yml

So this is not the same /aerolit_rhel8_standard issue anymore. The repo preflight worked, NFS got past the old export problem, and now the PXE Ansible playbook is failing with rc=2.

The deploy output is hiding the useful Ansible failure details, so do not rerun the whole deploy yet. Run just the PXE playbook directly so we can see the real error.

Run this exactly:

cd /opt/aerolit/aerolit-bundle/collections-hub/ansible
export ANSIBLE_CONFIG="$PWD/ansible.cfg"
set -o pipefail
ansible-playbook \
  -i localhost, \
  -c local \
  -e @/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_server_generated.yml \
  -e @/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml \
  /opt/aerolit/aerolit-bundle/collections-hub/ansible/pxe_server.yml \
  -vv \
  2>&1 | tee /tmp/aerolit-pxe-server-failure.log
echo "ansible rc=${PIPESTATUS[0]}"

Then run:

tail -n 120 /tmp/aerolit-pxe-server-failure.log

That should show the actual task or variable that failed.

Also run this quick check before anything else:

ls -l /opt/aerolit/aerolit-bundle/collections-hub/ansible/pxe_server.yml
ls -l /opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_server_generated.yml
ls -l /opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml

What I can say from your screenshot right now:

Phase 5 repo wiring is doing what we wanted. It found repos, ensured prereqs, refreshed repo vars, and passed both vars files into Ansible. The failure is now inside the existing pxe_server.yml execution path. Next step is finding the real Ansible task failure from that log.












































































REPO_VARS="/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml"
BACKUP="${REPO_VARS}.edge-test-backup"

rm -f "$BACKUP"
mv "$REPO_VARS" "$BACKUP"

aerolitctl deploy server \
  --dry-run \
  --skip-server-prereqs \
  --skip-interface \
  --skip-nfs \
  --skip-firewall \
  --skip-pxe \
  --skip-controller

RC=$?

mv "$BACKUP" "$REPO_VARS"

echo "deploy exit code was: $RC"




aerolitctl deploy server --dry-run --server-bootstrap-only






















After making that change, run this
python3 -m compileall src tools
python3 -m py_compile src/aerolitctl/deploy_server.py

Then rerun the missing-file edge test:

REPO_VARS="/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml"
BACKUP="${REPO_VARS}.edge-test-backup"

rm -f "$BACKUP"
mv "$REPO_VARS" "$BACKUP"

aerolitctl deploy server --dry-run --skip-repo-vars-refresh
RC=$?

mv "$BACKUP" "$REPO_VARS"

echo "deploy exit code was: $RC"

Expected now:

ERROR: Repository vars refresh was skipped, but repo vars file is missing:
deploy exit code was: 1

That proves the edge case is fixed.

































































Yes — test it by temporarily hiding the generated repo vars file, then running deploy with --skip-repo-vars-refresh.

The file you need to simulate as missing is:

/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml

Run this exactly:

REPO_VARS="/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml"
BACKUP="${REPO_VARS}.edge-test-backup"
mv "$REPO_VARS" "$BACKUP"
aerolitctl deploy server --dry-run --skip-repo-vars-refresh
RC=$?
mv "$BACKUP" "$REPO_VARS"
echo "deploy exit code was: $RC"

What you want after the improvement is:

ERROR: Repository vars refresh was skipped, but repo vars file is missing:
  /opt/aerolit/.../aerolitctl_repos_generated.yml
Run without --skip-repo-vars-refresh, or run:
  aerolitctl configure repos --refresh

And the exit code should be:

deploy exit code was: 1

What you do not want is for it to continue and show Ansible dry-run commands using:

-e @/opt/aerolit/.../aerolitctl_repos_generated.yml

when that file is missing.

After restoring the file, confirm normal behavior still works:

aerolitctl deploy server --dry-run --skip-repo-vars-refresh

Expected after restore:

Refresh repo vars               : no

and no error, because the file exists.

One more useful test: hide the file again, but do not skip refresh:

REPO_VARS="/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml"
BACKUP="${REPO_VARS}.edge-test-backup"
mv "$REPO_VARS" "$BACKUP"
aerolitctl deploy server --dry-run
RC=$?
mv "$BACKUP" "$REPO_VARS"
echo "deploy exit code was: $RC"

Expected there: it should not fail, because refresh is enabled. In dry-run it should say something like:

Would create: generated repository vars

That pair of tests proves both sides:

--skip-repo-vars-refresh + missing file = fail clearly
normal repo prep + missing file = generate/would-generate it















8. Test Phase 5 dry-run first

Run these from the repo root:

python3 -m compileall src tools
python3 -m py_compile src/aerolitctl/deploy_server.py
python3 -m py_compile src/aerolitctl/cli.py

Then confirm the new deploy flags exist:

aerolitctl deploy server --help | grep -E 'skip-repo-prep|skip-server-prereqs|skip-repo-vars-refresh|server-bootstrap-only'

Expected flags:

--skip-repo-prep
--skip-server-prereqs
--skip-repo-vars-refresh
--server-bootstrap-only

Then run the real Phase 5 proof command:

aerolitctl deploy server --dry-run --server-bootstrap-only

Expected new behavior:

# AeroLIT deploy repository preflight
Client bundle path              : /home/unix/sysadmin
Repo vars file                  : /opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml
Repos discovered                : 3
Ensure server prereqs           : yes
Refresh repo vars               : yes
Server bootstrap only           : yes

Then it should show either:

All server prerequisite packages are already installed.

or:

Would install missing packages from AeroLIT server bootstrap repo: ...

And the Ansible dry-run command should include both vars files:

-e @/opt/aerolit/aerolit-bundle/collections-hub/ansible/...server generated vars...
-e @/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml

That means Phase 5 is wired correctly.
























I would not add more code yet. Run the tests now.

Use this order:

python3 -m compileall src tools
python3 -m py_compile src/aerolitctl/deploy_server.py
python3 -m py_compile src/aerolitctl/cli.py

Then:

aerolitctl deploy server --help | grep -E 'skip-repo-prep|skip-server-prereqs|skip-repo-vars-refresh|server-bootstrap-only'

Then the important Phase 5 dry-run:

aerolitctl deploy server --dry-run --server-bootstrap-only

What you want to confirm:

# AeroLIT deploy repository preflight

and:

Client bundle path
Repo vars file
Repos discovered
Ensure server prereqs
Refresh repo vars
Server bootstrap only

Then look lower in the dry-run output for the Ansible command. It should include both vars files:

-e @...aerolitctl_server_generated...
-e @...aerolitctl_repos_generated.yml

One small note: even if compile passes, the real proof is the dry-run output. The code placement looks right, but the dry-run tells us whether the saved server config, repo discovery, generated vars path, and playbook command all line up together.






















































































PKG="$(
  for p in tree wget rsync lsof vim-enhanced bind-utils tcpdump unzip policycoreutils-python-utils; do
    if ! rpm -q "$p" >/dev/null 2>&1 && \
       dnf repoquery --disablerepo='*' --enablerepo='aerolit-server-bootstrap-*' "$p" >/dev/null 2>&1; then
      echo "$p"
      break
    fi
  done
)"

echo "Test package: $PKG"




aerolitctl configure repos \
  --ensure-server-prereqs \
  --server-bootstrap-only \
  --server-prereq-package "$PKG" \
  --dry-run



aerolitctl configure repos \
  --ensure-server-prereqs \
  --server-bootstrap-only \
  --server-prereq-package "$PKG"

rpm -q "$PKG"















































































3. Run compile checks

From your repo root:

python3 -m compileall src tools
python3 -m py_compile src/aerolitctl/repos.py
python3 -m py_compile src/aerolitctl/cli.py

Expected: no errors.

4. Confirm the new help flags exist

Run:

aerolitctl configure repos --help | grep -E 'write-server-bootstrap|server-bootstrap-repo-file|ensure-server-prereqs|server-prereq-package|server-bootstrap-only'

Expected to show all five:

--write-server-bootstrap
--server-bootstrap-repo-file
--ensure-server-prereqs
--server-prereq-package
--server-bootstrap-only
5. Re-test Phases 1–3

Run:

aerolitctl configure repos --show
aerolitctl configure repos --validate
aerolitctl configure repos --refresh --dry-run
aerolitctl configure repos --refresh

Expected:

RHEL 8 repos discovered.
RHEL 7/9/10 absent repos show as [INFO], not [WARN].
aerolitctl_repos_generated.yml is created/updated.
No failures.
6. Re-test Phase 4 manual repo writing

Dry run first:

aerolitctl configure repos --write-server-bootstrap --dry-run

Then real write:

aerolitctl configure repos --write-server-bootstrap

Then verify:

cat /etc/yum.repos.d/zz-aerolit-server-bootstrap.repo

dnf repoquery \
  --disablerepo='*' \
  --enablerepo='aerolit-server-bootstrap-*' \
  ansible-core podman nfs-utils dnsmasq firewalld

Expected: it should resolve those packages from the AeroLIT bootstrap repos.

7. Test the new Phase 4 automatic fallback

Dry run first:

aerolitctl configure repos --ensure-server-prereqs --dry-run

Then test bootstrap-only dry run:

aerolitctl configure repos --ensure-server-prereqs --server-bootstrap-only --dry-run

Then real run:

aerolitctl configure repos --ensure-server-prereqs --server-bootstrap-only

That command is the real Phase 4 completion test. It means:

Check missing packages.
Use the generated AeroLIT server bootstrap repo.
Clean metadata.
Install missing packages.
Leave the repo configured.

Afterward, verify:

rpm -q ansible-core podman nfs-utils dnsmasq firewalld
Status after these changes

After this, I would mark it:

Phase 1: done
Phase 2: done
Phase 3: done
Phase 4: done
Phase 5: not started
Phase 6: not started

Phase 5 is next: wiring this into deploy server so the normal happy path can just be:

aerolitctl deploy server









































































dnf repoquery --disablerepo='*' --enablerepo='aerolit-server-bootstrap-*' ansible-core podman nfs-utils dnsmasq firewalld

dnf list --available --disablerepo='*' --enablerepo='aerolit-server-bootstrap-*' ansible-core podman nfs-utils dnsmasq firewalld




dnf clean all
rm -rf /var/cache/dnf/aerolit-server-bootstrap-*
dnf makecache --disablerepo='*' --enablerepo='aerolit-server-bootstrap-*'


BASE=/opt/aerolit/aerolit-bundle/extracted_isos/rhel8iso

for repo in BaseOS AppStream; do
  echo "== $repo =="
  cd "$BASE/$repo" || exit 1

  href=$(sed -n 's/.*href="\([^"]*productid\.gz\)".*/\1/p' repodata/repomd.xml | head -n1)

  echo "productid href: $href"

  if [ -n "$href" ]; then
    ls -lh "$href"
    sha256sum "$href"
    gzip -t "$href" && echo "gzip OK" || echo "gzip FAILED"
  else
    echo "No productid.gz entry found in repomd.xml"
  fi

  echo
done




mkdir -p /mnt/rhel8iso

mount -o loop /path/to/rhel8.iso /mnt/rhel8iso

rm -rf /opt/aerolit/aerolit-bundle/extracted_isos/rhel8iso
mkdir -p /opt/aerolit/aerolit-bundle/extracted_isos/rhel8iso

cp -a /mnt/rhel8iso/. /opt/aerolit/aerolit-bundle/extracted_isos/rhel8iso/

umount /mnt/rhel8iso








dnf clean all
rm -rf /var/cache/dnf/aerolit-server-bootstrap-*
dnf makecache --disablerepo='*' --enablerepo='aerolit-server-bootstrap-*'












dnf makecache --disablerepo='*' --enablerepo='aerolit-server-bootstrap-*'

dnf list --disablerepo='*' --enablerepo='aerolit-server-bootstrap-*' \
  podman nfs-utils dnsmasq firewalld

dnf list --disablerepo='*' --enablerepo='aerolit-server-bootstrap-*' ansible-core









aerolitctl configure repos --write-server-bootstrap \
  --server-bootstrap-repo-file /tmp/zz-aerolit-server-bootstrap.repo

cat /tmp/zz-aerolit-server-bootstrap.repo

aerolitctl configure repos --write-server-bootstrap

cat /etc/yum.repos.d/zz-aerolit-server-bootstrap.repo
dnf repolist | grep aerolit













python3 -m py_compile src/aerolitctl/repos.py
python3 -m py_compile src/aerolitctl/cli.py

aerolitctl configure repos --help | grep -E 'write-server-bootstrap|server-bootstrap-repo-file'
aerolitctl configure repos --show
aerolitctl configure repos --refresh --dry-run
aerolitctl configure repos --write-server-bootstrap --dry-run

python3 - <<'PY'
import yaml
path = "/opt/aerolit/aerolit-bundle/collections-hub/ansible/group_vars/all/aerolitctl_repos_generated.yml"
with open(path) as f:
    data = yaml.safe_load(f)
print(data["aerolitctl_server_bootstrap_repos"]["os_key"])
print(len(data["aerolitctl_provisioning_repositories"]["rhel8"]))
PY




python3 -m compileall src
aerolitctl configure repos --help
aerolitctl configure repos --show
aerolitctl configure repos --validate
sudo aerolitctl configure repos --dry-run --refresh






This pxe.py you pasted does define print_pxe_status_summary() correctly at top level.

So if this exact file is actually saved at:

/root/aerolitctl/src/aerolitctl/pxe.py

then this specific import error should be gone.

Run this now:

cd /root/aerolitctl
grep -n "^def print_pxe_status_summary" src/aerolitctl/pxe.py
grep -n "^def pxe_status_snapshot" src/aerolitctl/pxe.py
grep -n "^def pxe_doctor_lines" src/aerolitctl/pxe.py

You should see all 3 functions.

Then run:

PYTHONPATH=/root/aerolitctl/src python3 - <<'PY'
from aerolitctl.pxe import print_pxe_status_summary, pxe_doctor_lines
print("PXE imports OK")
PY

Then:

PYTHONPATH=/root/aerolitctl/src python3 - <<'PY'
from aerolitctl.cli import main
print("CLI import OK")
PY

If those pass, rerun:

python3 tools/aerolitctl_merge_readiness.py

One more thing: after this import problem is fixed, your test may fail later because your test bans this string:

"PXE:"

But the doctor.py change we added likely contains:

report.add_ok("PXE: {}".format(message))
report.add_warning("PXE: {}".format(message))
report.add_failure("PXE: {}".format(message))

So either change the test ban from:

bad_strings = [
    "NFS/controller",
    "PXE:",
]

to:

bad_strings = [
    "NFS/controller",
]

or change doctor.py to avoid the colon wording, for example:

report.add_ok("PXE {}".format(message))

I’d remove "PXE:" from the banned strings. "PXE:" is not old/confusing wording the same way "NFS/controller" is. It’s a normal prefix for doctor output.

But for the failure in your screenshot: this pasted pxe.py should fix it. If it still fails, the VM is not running this saved file. Use this to prove what Python is importing:

PYTHONPATH=/root/aerolitctl/src python3 - <<'PY'
import aerolitctl.pxe as pxe
print(pxe.__file__)
print("print_pxe_status_summary:", hasattr(pxe, "print_pxe_status_summary"))
print("pxe_doctor_lines:", hasattr(pxe, "pxe_doctor_lines"))
PY
